// ========== 任务路由 ==========
const express = require("express");
const router = express.Router(); // 创建路由实例
const db = require("../db");     // 引入数据库实例

// TODO: 后续你来补充以下接口 ↓
// router.post("/lists/:listId/tasks", ...)  → 在指定列表下新增任务
// router.put("/:id", ...)                   → 修改任务
// router.patch("/:id", ...)                 → 切换完成/星标状态
// router.delete("/:id", ...)                → 删除任务

module.exports = router;
